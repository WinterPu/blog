import { ArticleCardDirective } from './article-card.directive';

describe('ArticleCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ArticleCardDirective();
    expect(directive).toBeTruthy();
  });
});
